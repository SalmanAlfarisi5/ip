/**
 * The graphical front end.
 * <p>
 * The window shows the conversation and passes what the user types to the
 * chatbot, which answers with text. Nothing here decides what a command does:
 * it only displays the reply and styles it by the kind of command that produced
 * it.
 */
package sallman.gui;
